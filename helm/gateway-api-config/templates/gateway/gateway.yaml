{{- range $i, $gateway := .Values.gateways }}
{{- if $gateway.enabled }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ $gateway.name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "labels.common" $ | nindent 4 }}
    {{- with $gateway.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $gateway.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gatewayClassName: {{ $gateway.className }}
  {{- if dig "envoyProxy" "enabled" true $gateway }}
  infrastructure:
    parametersRef:
      group: gateway.envoyproxy.io
      kind: EnvoyProxy
      name: gateway-{{ $gateway.name }}
      namespace: {{ $.Release.Namespace }}
  {{- end }}
  {{- if dig "allowedListeners" "enabled" false $gateway }}
  allowedListeners:
    namespaces:
      from: {{ dig "allowedListeners" "namespaces" "from" "All" $gateway }}
      {{- with (dig "allowedListeners" "namespaces" "selector" dict $gateway) }}
      selector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- end }}
  listeners:
    {{- range $k, $l:= $gateway.listeners }}
    - name: {{ $l.name }}
      protocol: {{ $l.protocol }}
      port: {{ $l.port }}
      {{- with $l.allowedRoutes }}
      allowedRoutes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $l.hostname }}
      hostname: {{ tpl (. | quote) $ }}
      {{- end }}
      {{- with $l.tls }}
      tls:
        mode: {{ .mode }}
        {{- if or (.certificateRefs) (dig "certificate" "enabled" false $l) }}
        certificateRefs:
        {{- if and (eq .mode "Terminate") (dig "certificate" "enabled" false $l) }}
        - kind: Secret
          name: {{ printf "gateway-%s-%s-tls" $gateway.name $l.name }}
        {{- end }}
        {{- range .certificateRefs }}
        - kind: Secret
          name: {{ .name }}
          {{- if .namespace }}
          namespace: {{ .namespace }}
          {{- end }}
        {{- end }}
        {{- end }}
      {{- end }}
    {{- end }}
{{- end }}
{{- end }}
